package test.runner

class MessagePrinterTest {
  def runMessagePrintTest {
    val messagePrinter = new com.horstmann.MessagePrinter()
    val arrayBuffer = new collection.mutable.ArrayBuffer()
    messagePrinter.print
  }
}

